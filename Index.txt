<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>HK Cashloans</title>
<style>
  /* Reset and base styles */
  *, *::before, *::after {
    box-sizing: border-box;
  }
  body {
    margin: 0;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background: #f9fafb;
    color: #1a202c;
    line-height: 1.6;
    scroll-behavior: smooth;
  }
  a {
    text-decoration: none;
    color: #2c7a7b;
    transition: color 0.3s ease;
  }
  a:hover, a:focus {
    color: #234e52;
  }
  header {
    background: #2c7a7b;
    color: white;
    padding: 1rem 2rem;
    position: sticky;
    top: 0;
    z-index: 1000;
    box-shadow: 0 2px 5px rgba(0,0,0,0.1);
  }
  .container {
    max-width: 1100px;
    margin: 0 auto;
  }
  nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
  .logo {
    font-size: 1.75rem;
    font-weight: 700;
    letter-spacing: 2px;
  }
  nav ul {
    list-style: none;
    display: flex;
    gap: 1.5rem;
  }
  nav button.cta {
    background: #e53e3e;
    border: none;
    color: white;
    padding: 0.5rem 1.25rem;
    font-size: 1rem;
    border-radius: 4px;
    cursor: pointer;
    transition: background 0.3s ease;
  }
  nav button.cta:hover, nav button.cta:focus {
    background: #c53030;
  }

  /* Hero Section */
  .hero {
    background: linear-gradient(135deg, #2c7a7b 0%, #81e6d9 100%);
    color: white;
    padding: 6rem 2rem 4rem;
    text-align: center;
  }
  .hero h1 {
    font-size: 3rem;
    margin-bottom: 0.5rem;
    text-shadow: 1px 1px 4px rgba(0,0,0,0.3);
  }
  .hero p {
    font-size: 1.25rem;
    max-width: 600px;
    margin: 0 auto 2rem;
    font-weight: 600;
  }
  .hero button {
    background: #e53e3e;
    border: none;
    color: white;
    padding: 0.75rem 2rem;
    font-size: 1.25rem;
    border-radius: 4px;
    cursor: pointer;
    box-shadow: 0 4px 10px rgb(229 62 62 / 0.5);
    transition: background 0.3s ease;
  }
  .hero button:hover, .hero button:focus {
    background: #c53030;
  }

  /* Sections */
  section {
    padding: 4rem 2rem;
  }
  h2 {
    text-align: center;
    font-size: 2.25rem;
    margin-bottom: 2rem;
    color: #2c7a7b;
  }
  .about, .services, .testimonials, .contact {
    max-width: 900px;
    margin: 0 auto;
  }
  .about p {
    font-size: 1.1rem;
    max-width: 700px;
    margin: 0 auto;
    text-align: center;
    color: #4a5568;
  }
  /* Services cards */
  .services-list {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 2rem;
  }
  .service-card {
    background: white;
    border-radius: 8px;
    padding: 1.75rem 2rem;
    width: 280px;
    box-shadow: 0 4px 10px rgb(0 0 0 / 0.1);
    transition: transform 0.